{
  "name": "ref-tool-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for GRA",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --platform=node",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "esbuild": "^0.27.0",
    "wrangler": "^4.47.0"
  }
}
